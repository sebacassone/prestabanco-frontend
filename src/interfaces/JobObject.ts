/**
 * Interface for Job object
 * @interface
 * @property {string} seniorityJob - The seniority of the job
 * @property {string} activity - The activity of the job
 */
interface Job {
  seniorityJob: string;
  activity: string;
}

export default Job;
